#include <string.h>
#include <stdio.h>

int main()
{
	char str1[] = "hello world";
	char str2[] = "";

	fprintf(stdout, "%s\n", str1);
	fprintf(stdout, "%s\n", str2);
	
	strncpy(
	str2 = "1";
	fprintf(stdout, "%s\n", str2);
}
